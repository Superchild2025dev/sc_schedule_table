{
  "name": "sc-schedule-firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore --project sc-schedule-rules-test \"node --test ../../tests/firestore-rules-emulator.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
